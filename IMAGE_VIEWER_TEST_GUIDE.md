# 正式课程图片查看器快速测试指南

## 测试环境要求
- 浏览器：Chrome, Safari, Firefox 或 Edge
- 设备：PC、iPad或手机
- 已登录系统并有token余额

## 快速测试步骤

### 测试1：模块一 - 图片生成

1. 访问任意正式课程，例如：
   ```
   http://localhost:5000/sunguo-formal/formal_hairstyle
   ```

2. 在"模块一：图片生成"中：
   - 在提示词输入框输入：`长发、黑色、微卷、笑容甜美`
   - 选择2-3种风格（如：真实照片、卡通可爱、动漫）
   - 点击"生成图片"按钮

3. 等待图片生成完成后：
   - ✅ 验证：每张图片上鼠标悬停时显示手指光标（cursor: pointer）
   - ✅ 验证：点击第1张图片，能打开全屏查看器
   - ✅ 验证：在查看器中点击右箭头，切换到第2张图片
   - ✅ 验证：继续点击右箭头，切换到第3张图片
   - ✅ 验证：点击左箭头，能返回到第2张
   - ✅ 验证：按ESC键或点击关闭按钮，关闭查看器

### 测试2：模块二 - 课后小游戏

1. 在"模块二：课后小游戏"中：
   - 点击"拍照或上传照片"，上传一张人物照片
   - 点击"拍照或上传画作"，上传一张手绘作品
   - 选择1-2种风格
   - 点击"生成图片"按钮

2. 等待组合图片生成后：
   - ✅ 验证：点击任意生成的图片，能打开查看器
   - ✅ 验证：查看器中包含模块一和模块二的所有图片
   - ✅ 验证：可以使用左右箭头在所有图片间切换

### 测试3：模块三 - AI点评作品

1. 在"模块三：AI点评作品"中：
   - 点击"上传作品"，上传一张手绘作品
   - 等待AI点评生成

2. 收到点评后：
   - 点击"生成改良版"按钮
   - 等待改良版图片生成

3. 查看对比效果：
   - ✅ 验证：原作品和改良版图片都有手指光标
   - ✅ 验证：点击原作品，能在查看器中打开
   - ✅ 验证：点击右箭头，能切换到改良版图片
   - ✅ 验证：查看器中包含页面上所有生成的图片（模块一+二+三）

### 测试4：键盘和触摸操作

1. PC端键盘测试：
   - ✅ 打开查看器后，按左方向键←，能切换到上一张
   - ✅ 按右方向键→，能切换到下一张
   - ✅ 按ESC键，能关闭查看器

2. 移动端触摸测试（iPad/手机）：
   - ✅ 在图片上左滑，切换到下一张
   - ✅ 在图片上右滑，切换到上一张
   - ✅ 双击图片，关闭查看器

### 测试5：边界情况

1. 只有一张图片：
   - ✅ 验证：左右箭头按钮可以点击但不会出错
   - ✅ 验证：查看器显示"1/1"

2. 多次生成图片：
   - 先在模块一生成3张图片
   - 再在模块二生成2张图片
   - 最后在模块三生成改良版（共2张：原图+改良版）
   - ✅ 验证：点击任意图片，查看器包含所有7张图片
   - ✅ 验证：图片顺序为：模块一(3张) → 模块二(2张) → 模块三(2张)

3. 打印功能：
   - ✅ 验证：在查看器中点击打印按钮，能打开打印预览

## 预期结果

### 控制台日志
在浏览器开发者工具的Console中，应该看到：

```
✅ 图片查看器已初始化
✅ 正式课程图片查看器已初始化
🖼️ 找到 X 张启用viewer的图片
📸 打开图片查看器: Y/X
```

### 视觉效果
- 所有生成的图片显示手指光标（cursor: pointer）
- 点击图片后平滑过渡到全屏查看
- 左右箭头按钮清晰可见
- 图片计数器显示当前位置（如"3/7"）

## 常见问题排查

### Q1: 点击图片没有反应
**排查步骤：**
1. 打开浏览器开发者工具（F12）
2. 查看Console是否有错误信息
3. 检查是否看到"✅ 图片查看器已初始化"日志
4. 刷新页面重试

### Q2: 图片顺序不对
**可能原因：**
- 图片是异步生成的，后生成的图片会排在后面
- 这是正常的，符合从上到下、从左到右的DOM顺序

### Q3: 左右切换没反应
**排查步骤：**
1. 检查Console是否有"ImageViewer 未加载"的警告
2. 检查 sunguo_class.js 是否正确加载
3. 刷新页面重试

### Q4: 移动端滑动不流畅
**可能原因：**
- 网络较慢，图片加载中
- 建议等待图片完全加载后再滑动

## 测试完成标准

- ✅ 三个模块的图片都能启用viewer
- ✅ 左右切换功能正常
- ✅ 键盘和触摸操作都支持
- ✅ 图片顺序符合从上到下、从左到右
- ✅ 打印功能正常
- ✅ 关闭功能正常
- ✅ 没有JavaScript错误

## 性能指标

- 图片加载时间：< 3秒（取决于网络）
- 切换响应时间：< 100ms
- 打开/关闭动画：流畅无卡顿

---

**测试愉快！** 🎉
