# iPad界面使用指南

## 🎨 全新iPad布局设计

为了更好地适配iPad和手写笔使用，我们重新设计了界面布局：

### 📱 界面布局特点

1. **顶部控制栏**（紧凑设计）
   - 分辨率选择器
   - 提示词输入框
   - 生成按钮

2. **全屏画布区域**
   - 占据主要屏幕空间
   - 适合手写笔绘图
   - 自适应画布尺寸

3. **侧滑工具栏**（按需显示）
   - 默认隐藏，不占用画布空间
   - 点击右下角悬浮按钮打开
   - 从右侧滑出，方便访问所有工具

### 🎯 使用方法

#### 打开工具栏
1. 点击屏幕右下角的**紫色圆形悬浮按钮**（带工具图标）
2. 工具栏从右侧滑出

#### 选择工具
1. 在工具栏中选择你需要的工具：
   - **绘图工具**：画笔/铅笔/马克笔/喷雾
   - **笔触类型**：圆形/方形
   - **画笔大小**：拖动滑块或点击预设（极细/细/中/粗/极粗）
   - **颜色**：12色快速选择 + 自定义颜色
   - **形状工具**：直线/矩形/圆形/箭头

2. 选择完成后，工具栏会**自动收起**，不影响绘图

#### 关闭工具栏
- 点击工具栏右上角的 **×** 按钮
- 或点击工具栏外的任意区域
- 或选择工具后自动关闭

#### 压感指示器
- 位于屏幕左上角（顶部控制栏下方）
- 实时显示Apple Pencil的压力值
- 颜色变化：绿色(轻) → 橙色(中) → 红色(重)

### 🖌️ 绘图操作

1. **开始绘图**
   - 用Apple Pencil直接在画布上绘制
   - 支持压感：按压力度控制线条粗细

2. **调整画笔**
   - 点击悬浮按钮 → 选择工具 → 工具栏自动收起
   - 继续绘图

3. **撤销/重做**
   - 在工具栏中找到撤销/重做按钮
   - 支持50步历史记录

4. **清空画布**
   - 工具栏底部的"清空"按钮
   - 会提示确认

### 📐 屏幕方向支持

#### 横屏模式（推荐）
- 工具栏在右侧
- 画布居中显示
- 适合长时间绘图

#### 竖屏模式
- 工具栏在右侧（宽度自适应）
- 画布自动适配
- 适合单手操作

### ⚙️ 优化特性

#### 触摸优化
- **最小触摸目标**：44x44px（符合Apple设计规范）
- **无高亮效果**：触摸时不显示蓝色高亮
- **禁用长按菜单**：不会弹出复制粘贴菜单
- **平滑滚动**：工具栏支持流畅滑动

#### 压感增强
- 支持 iOS `force` API
- 支持 Android `webkitForce` API
- 0.1-1.0 压力范围自动归一化
- 实时视觉反馈

#### 布局适配
- 1:1 显示比例，无需缩放
- 防止意外缩放（user-scalable=no）
- 全屏显示（viewport-fit=cover）
- 状态栏半透明效果

### 🔧 故障排除

#### 工具栏没有显示
- 检查是否在iPad上访问（设备检测）
- 刷新页面

#### 悬浮按钮看不见
- 可能被工具栏遮挡，先关闭工具栏
- 检查是否被其他元素覆盖

#### 压感不工作
1. 确认使用的是 **Apple Pencil**（不是手指）
2. 查看左上角压感指示器是否显示
3. 打开浏览器控制台（Safari开发者菜单）
4. 查看压感检测日志：
   ```
   === 设备检测 ===
   === 压感测试 ===
   force: 0.5
   ```

#### 画布太小
- 在顶部控制栏选择合适的分辨率
- 点击"应用分辨率"按钮
- 推荐iPad横屏使用 1024×576 或 1024×1024

#### 工具栏滚动不流畅
- 工具栏支持垂直滚动
- 用单指上下滑动访问更多工具

### 💡 使用技巧

1. **快速切换工具**
   - 打开工具栏 → 点击工具 → 自动关闭（0.3秒延迟）
   - 无需手动关闭，提升效率

2. **颜色历史**
   - 最近使用的10种颜色自动保存
   - 快速重复使用常用颜色

3. **形状工具**
   - 选择形状工具后，在画布上拖拽绘制
   - 松开后形状固定

4. **画笔预设**
   - 5个快速大小预设：极细(2px) 细(5px) 中(10px) 粗(20px) 极粗(40px)
   - 点击即可切换，无需拖动滑块

### 📊 设备检测信息

打开Safari浏览器的开发者控制台（设置 → Safari → 高级 → Web检查器），可以看到：

```javascript
=== 设备检测 ===
触摸设备: true
iOS设备: true
User Agent: Mozilla/5.0 (iPad; CPU OS 17_0...)

=== 压感测试 ===
force: 0.5
webkitForce: undefined
压感支持: ✅ 是
```

### 🎉 完成

现在您可以在iPad上享受流畅的手绘体验了！

如有问题，请检查：
1. ✅ 使用的是iPad（768-1366px宽度）
2. ✅ 使用Safari或Chrome浏览器
3. ✅ 网络连接正常
4. ✅ 使用Apple Pencil绘图（支持压感）
